// Generated by CoffeeScript 1.10.0
(function() {
  $(document).ready(function() {
    $.each($("nav li"), function(index, el) {
      var xx;
      xx = $(el).find("a").attr("href");
      if (window.location.pathname === "/" + xx) {
        return $(el).addClass("current");
      }
    });
    $(".products .block ul a, .products .frames a").click(function(e) {
      var id;
      e.preventDefault();
      id = $(this).attr("href");
      $(".categories").hide();
      $(".categories" + id).show();
      return $(".frames").hide();
    });
    return $(".products .thumbnails img").click(function(e) {
      var src;
      e.preventDefault();
      src = $(this).attr("src");
      return $(".products .categories:visible .main_img").attr("src", src);
    });
  });

}).call(this);
